Currently Working On:
    1.2 Allow user to delete profile/account
2. Add Admin/Branch/Driver login based on role with Views to set access restrictions
    2.1 Deletion is managed by the DBMS so only allow users to update info
3. Add restaurants page which displays all restaurants to users and allows navigation to menu selection
    3.1 Basket feature
    3.2 Create order transaction and add transaction to database

Bugs:
1. Possible session issues and code redundancy

Upcoming To Do:
1. Password salting
2. Prepared statements in register/login